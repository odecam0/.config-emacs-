;; (find-metafile "new.el" "( the SEXPS THAT DEFINE A GIVEN REGION")

;; ( A sexp deve ser tal qual as as sexps que são escritas com o eev, porém,
;; ( quando chamadas, junto de um parâmetro numérico, devem dar narrow em uma
;; ( região que envolver os próximos N parágrafos, a partir de onde o link bateu.


